<!-- vocab-web 프로젝트 작업 일지 (세션 연속성용) -->
# context-notes.md

## 2026-07-08 (이어서) — 다음 작업 예고: 반 명단 자동화를 엑셀 파일 출력 방식으로 전환

사용자가 단어장 입력 화면 개선 작업 도중에 다음 작업을 미리 알려줌 ("단어장 정리되면 반 명단 문서 자동화 페이지도 수정 필요"). AI 메모리 시스템에도 저장해둠(`roster_excel_file_output` 메모리).

**요청 내용**: 지금 `/roster`는 전체명단 탭 하나만 업로드받아 5개 반 유형 전부를 웹 화면 표로 보여주는데(드래그해서 복사), 이걸 아래처럼 바꾸고 싶어함.
1. 업로드 파일 자체가 여러 탭(전체명단 + 문서별 빈 서식 탭들)으로 구성됨.
2. 전체명단 탭 데이터를 기준으로 나머지 탭의 이름에 맞춰 정확히 채움.
3. 웹 표가 아니라 **채워진 엑셀 파일을 실제로 다운로드**할 수 있게 전달.
4. 업로드된 파일에 실제 존재하는 탭/반만 처리 (예: "실전" 탭만 있으면 토익 실전 오전/오후만 하고 나머지는 건너뜀 — 지금처럼 5개 유형 무조건 다 처리하지 않음).

**다음에 할 일**: 단어장 입력 화면 개선(Task 1~6) 끝난 뒤, 이 요청으로 브레인스토밍부터 다시 시작. 기존 `lib/roster/classify.ts`/`documents.ts`/`verify.ts`의 반 분류·검증 로직은 재사용 가능하나, 입력(다중 탭 엑셀)과 출력(엑셀 파일 생성) 방식은 기존 설계 문서에서 범위 밖으로 뺐던 부분이라 재설계 필요.

## 2026-07-08 (이어서) — 단어 시험 채점 버그 수정 (띄어쓰기/다중 뜻)

### 한 일

- 사용자가 배포된 `/grading` 기능에서 3가지 문제 제보: (1) 답안 이미지/엑셀 업로드 원함 (2) 띄어쓰기 유무로 오답 처리됨 (3) 다중 뜻 단어 채점 미숙.
- systematic-debugging으로 (2)(3)을 실제 OpenAI API(gpt-4o-mini) 직접 호출로 재현: 20단어 배치 테스트 3회 반복.
  - (2) 원인: 채점 기준의 "복합명사/구의 일부 의미 요소가 빠지면 오답" 규칙을, AI가 "붙여쓰기/띄어쓰기 차이"까지 "의미 요소 누락"으로 잘못 해석 (예: "학교 가방"→"학교가방" 오답 처리). 9개 테스트 케이스 모두 재현됨.
  - (3) 원인: "품사가 명백히 다르면 오답" 규칙 때문에, 한 단어에 서로 다른 품사의 뜻이 여러 개 등록된 경우(book=책(명사)/예약하다(동사)) 학생이 두 번째 뜻으로 정확히 답해도 "품사가 다르다"며 오답 처리. book/light/address/fine/right/produce 전부 재현됨.
- 두 문제를 동시에 고치는 프롬프트 수정안을 만들어 같은 방식으로 3회 재현 테스트 → 27개 중 26개, 18개 중 18개로 대폭 개선. 회귀 확인용 진짜 오답 케이스는 3회 모두 정상적으로 오답 유지.
- 사용자 승인 받아 `lib/grading/grade-student.ts`의 `GRADING_CRITERIA`에 두 규칙(띄어쓰기 무시, 다중 뜻 시 뜻별 품사 허용) 추가. 커밋(`3b496bb`) 및 푸시 완료.

### 결정사항과 이유

- **자동화 테스트 대신 1회성 재현 스크립트로 검증**: 채점은 실제 OpenAI 응답에 의존하는 비결정적 동작이라, 기존 vitest 테스트(클라이언트 mock)로는 "프롬프트가 실제로 잘 채점하는가"를 검증할 수 없음. `.env.local`의 실제 키로 3회 반복 호출하는 방식으로 Phase 1(재현)과 Phase 3(수정안 검증)을 모두 수행 — 이 스크립트는 임시용이라 저장소에 남기지 않음.
- **실제 서비스 영향 때문에 즉시 배포**: 현재 운영 중인 기능의 채점 정확도에 직접 영향을 주는 수정이라, 다음 세션까지 미루지 않고 사용자 확인 후 바로 푸시.

### 미완료 / 다음에 할 일

- [ ] 1번 문제(답안 이미지/엑셀 업로드) — 아직 요구사항 논의 전. 브레인스토밍 필요 (사진 한 장에 여러 학생 답안? 학생별 개별 사진? OCR 정확도 어떻게 보정할지 등).
- [ ] 3단계(반 명단) Task 7에서 남겨둔 항목: 브라우저에서 직접 `/roster` 사용해보며 표 드래그→복사 확인 (사용자 확인 필요).
- [ ] (이전 세션 이월) OpenAI 키 / Vercel Blob 토큰 재발급 여부 확인 — 아직 미해결.

## 2026-07-08 — 3단계(반 명단 문서 자동화) 계획 문서 작성

### 한 일

- writing-plans 스킬로 3단계 구현 계획 작성: `docs/superpowers/plans/2026-07-08-roster-docs-feature.md` (Task 1~7).
- 계획 작성 전 애매했던 부분 3가지를 사용자에게 직접 확인: (1) 반 표 구성은 "5개 반유형(기초/중급/실전/오픽중급/오픽실전) 기준"으로 결정 — 온라인 출석부만 오전+오후 결합, 나머지 3종 문서는 오전/오후 별도 표. "반 선택" 필터도 이 5개 유형 기준. (2) 테스트 데이터는 실제 샘플 파일 사용하기로 결정. (3) 신청 컬럼 자동 매칭 실패 시 수동 선택 화면을 이번 계획 범위에 포함하기로 결정.
- 사용자가 저장소 루트에 실제 샘플 명단 `test.xlsx`(실제 학생 개인정보 포함, 359명)를 추가함 → 시트 구조를 직접 열어서 확인: `1기 전체` 탭 기준 헤더는 5번째 행(0-indexed 4), 학생 데이터는 6번째 행부터. 컬럼 위치(0-indexed) 학번=2, 이름=3, 학과=5, 학년=6, 전화번호=7, `1기 신청`=8, `2기신청`=9. 반명 10종 전부 확인(기초/중급/실전 오전·오후, 오픽 중급/실전 오전·오후). `온라인 출석부` 탭에서 "수강반" 컬럼이 토익 반은 `"토익 " + 원본반명`으로, 오픽 반은 원본반명 그대로 표기되는 규칙을 실물로 확인.
- `test.xlsx`에 실제 학생 개인정보(학번/이름/전화번호)가 들어있어 커밋 위험이 있다고 판단 → 사용자 승인 받아 `.gitignore`에 `test.xlsx` 추가 (커밋 완료).
- 계획 문서의 유닛 테스트는 실제 파일이 아니라 코드 안 합성 픽스처를 쓰기로 결정(속도), 단 컬럼 배치는 실제 파일과 동일하게 맞춤. 실제 파일 기반 검증은 Task 7(수동 확인)에서 진행.

### 결정사항과 이유

- **반 표 구성을 "5개 유형" 기준으로**: 설계 문서에 "반(오전/오후 구분) × 문서종류" 표를 만든다면서도 온라인 출석부만 "오전/오후 나란히"라고 서로 다르게 적혀 있어 해석이 두 갈래였음 → 사용자에게 직접 확인. "반 선택" 필터 문구("해당 반(오전+오후)만 필터링")와도 일치하는 해석이라 5개 유형 기준으로 확정.
- **개인정보 파일 .gitignore 처리**: CLAUDE.md의 "개인정보를 다루지 않는다"는 이번 기능 자체의 설계 원칙(서버에 저장 안 함)이지만, 로컬 테스트 픽스처 파일이 git 이력에 영구히 남는 것은 별개의 위험이라 판단해 먼저 물어보고 처리함.

### 미완료 / 다음에 할 일

- [ ] 3단계 계획 실행 (Task 1~7) — subagent-driven-development 또는 executing-plans로 진행할지 다음 세션에서 사용자에게 확인 필요.
- [ ] (이전 세션 이월) OpenAI 키 / Vercel Blob 토큰 재발급 여부 확인.

### 막힌 점 / 사용자 결정 필요

- (해결됨) 계획 실행 방식은 서브에이전트 기반으로 진행하기로 확인받음. master에서 바로 작업(1·2단계와 동일 관례)하기로도 확인받음.

## 2026-07-08 (이어서) — 3단계(반 명단 문서 자동화) Task 1~7 전체 완료

### 한 일

- subagent-driven-development로 3단계 계획(Task 1~7) 전부 실행. master에서 직접 작업(1·2단계와 동일 관례, 사용자 확인받음).
- Task 1(반 유형 상수/타입/명단 파일 파싱): 리뷰 승인, 재작업 없음.
- Task 2(반 분류): 새 파일에 한국어 헤더 주석 누락 → 1회 수정 후 승인.
- Task 3(문서 생성): 리뷰 승인, 재작업 없음.
- Task 4(인원수/이름 검증): `classified[i]`/`documents[i]`를 배열 인덱스로만 짝지어서, 문서 생성 단계에서 배열이 어긋나면 크래시 나는 문제 발견 → `classType.id` 기준 매칭으로 수정, 매칭 실패 시에도 크래시 대신 "전체 누락" 경고를 내도록 수정 후 승인.
- Task 5(API 라우트): 클라이언트가 `columnIndex`를 직접 넘긴 경로에서 `parseRosterRows`가 try/catch 밖에 있어 양식 오류 파일이 400 대신 500으로 떨어지는 문제 발견 → try 블록을 넓혀 수정 후 승인.
- Task 6(화면 UI): 리뷰 승인, 재작업 없음.
- Task 7(수동 확인): 브라우저 대신 실제 개발 서버 + curl로 로그인 쿠키를 받아 `/api/roster/generate`를 실제 `test.xlsx`(359명, gitignored)로 직접 호출해서 검증. 1기 반별 인원수(기초47/20, 중급61/45, 실전55/16, 오픽중급32/29, 오픽실전26/17)가 실제 파일과 완전히 일치, 검증 경고 0건. 이주호(토익 중급 오전) 데이터 정확, 박신영은 1기 신청 컬럼이 비어 있어 1기 결과에서 제외되고 2기 신청(중급 오전)에는 정상 포함됨을 확인 — 설계 문서의 테스트 방향과 정확히 일치. 합성 파일로 형식 오류(400)와 컬럼 자동매칭 실패→수동 선택 흐름도 추가로 확인.
- 최종 전체 브랜치 리뷰(opus): "병합 가능(Yes)", Critical/Important 0건. Minor 중 하나(경고 표에 `toeic-mid`, `printAttendance` 같은 개발자용 식별자가 그대로 노출되는 문제 — 이 화면은 비전공자 교사/TA 대상)는 CLAUDE.md의 "비전공자 친화 응답" 원칙과 직결돼서 즉시 반영(한국어 라벨로 표시). 나머지 Minor(columnIndex 미검증, 빈 반 유형도 항상 렌더링, 중복 이름 시 missingNames 빈 배열, CSV 경로 미검증, 시트 이중 파싱)는 낮은 우선순위라 코드 변경 없이 백로그로만 남김.
- `npm run build` 최종 클린 확인.

### 결정사항과 이유

- **Task 7을 브라우저 대신 curl로 검증**: 이 환경에 브라우저 자동화 도구가 없어서, 로그인 → API 직접 호출 방식으로 백엔드 전체 흐름(정상/오류/수동선택 3개 경로)을 실제 데이터로 검증했다. 브라우저 드래그선택→복사 같은 순수 시각적 동작은 검증하지 못했음 — 사용자가 실제로 브라우저에서 한 번 써보고 이상 있으면 알려달라고 안내 필요.
- **경고 표 한국어 라벨은 최종 리뷰 직후 바로 반영**: 계획에는 없었지만 CLAUDE.md 전역 규칙(비전공자 친화)에 직접 걸리는 문제라 사용자에게 다시 묻지 않고 바로 수정 (2줄짜리 표시 전용 변경이라 위험 낮음).

### 미완료 / 다음에 할 일

- [x] GitHub 푸시 완료 (2026-07-08, `a82ab3d..f8e857b`, 13개 커밋). Vercel CLI가 이 환경에 없어 자동 배포 상태는 대시보드에서 직접 확인 필요.
- [ ] 배포 완료 후 https://toeic-summer.vercel.app/roster 에서 실제 브라우저로 표 드래그→복사→엑셀 붙여넣기가 되는지 확인 (curl 검증으로는 못 본 부분, 사용자 확인 필요).
- [ ] (이전 세션 이월) OpenAI 키 / Vercel Blob 토큰 재발급 여부 확인 — 아직 미해결.

### 막힌 점 / 사용자 결정 필요

- 없음. 3단계 코드는 완결된 상태, 배포 여부만 사용자 승인 대기.

## 2026-07-07 — 2단계(단어 시험 채점 기능) 전체 완료

### 한 일

- subagent-driven-development로 2단계 계획(Task 1~7) 전부 실행. Task 1~2는 직전 세션에서 이미 커밋까지 돼 있었고(리뷰만 안 된 상태), 이어서 Task 3~7 실행 및 각 태스크 리뷰+수정 루프 진행.
- Task 3(OpenAI 채점 로직)은 리뷰 3라운드: 응답 검증/순서 안정화 → 중복 단어 안전 처리 → 승인.
- Task 4(오답 집계)는 동점 처리 규칙 문서화+테스트로 1라운드 수정.
- Task 5(API 라우트)는 잘못된 요청 본문 에러 처리로 1라운드 수정.
- Task 6(화면 UI)은 중복 제출 방지 + 단어장 조회 실패 처리로 1라운드 수정.
- Task 7(수동 확인+배포) 중 로컬에서 실제 버그 발견: gpt-4o-mini가 JSON 응답을 마크다운 코드블록으로 감싸서 보내 모든 채점이 "수동 확인 필요"로 빠짐 → 코드블록 벗겨내는 수정.
- Task 7 진행 중 Vercel Blob 저장소가 private으로 생성돼 있어 저장 실패 → 사용자가 Public으로 재생성해서 해결 (설계상 단어장은 비개인정보라 public이 의도한 설계).
- 로컬 + 실제 배포 사이트(toeic-summer.vercel.app) 양쪽에서 5가지 흐름(로그인/단어장 저장·조회/답안 업로드/실채점) 전부 확인 완료.
- 최종 전체 브랜치 리뷰(opus)에서 Important 4건 발견 후 전부 반영:
  1. 학생 1명 OpenAI 호출 실패가 Promise.all 전체를 실패시키던 문제 → grade-student.ts에서 API 호출 자체를 try/catch로 감싸 그 학생만 수동확인 처리.
  2. loadWordList가 "저장 안됨"과 "네트워크 오류"를 구분 못 해 조회 실패 시 교사가 빈 단어장으로 덮어쓸 위험 → BlobNotFoundError만 null 처리, 나머지는 에러 전파 + 빈 단어장 저장 자체 금지.
  3. 답안 개수 불일치 경고가 계산만 되고 화면에 안 보이던 문제 → 결과 화면에 표시.
  4. 설계 문서의 "애매 항목 검토 UI"(AI가 애매하다고 판단한 항목을 교사가 그 자리에서 뒤집는 기능)가 2단계 계획 문서 작성 시 누락됨 → aggregate.ts + 결과 화면에 추가 구현.

### 결정사항과 이유

- **속도 우선 조정**: 사용자가 세션 중간에 "속도가 너무 느리다"고 명시적으로 요청 → 이후로는 사소한 fix는 재리뷰 서브에이전트를 또 띄우지 않고 diff를 직접 읽어 검증하는 방식으로 전환 (품질 게이트 자체를 생략하지는 않음).
- **애매 항목 UI는 지금 추가**: 원래 설계 문서의 핵심 기능인데 계획 단계에서 빠졌던 것이라, 나중으로 미루지 않고 최종 리뷰 직후 바로 구현하기로 사용자가 결정.
- **디버깅 중 보안 이슈**: 세션 중 제 실수로 `.env.local` 내용(OpenAI 키, Blob 토큰)이 터미널 로그에 그대로 출력된 적 있음 — 사용자에게 재발급 권장함. 다음 세션에서 재발급 여부 확인 필요.

### 미완료 / 다음에 할 일

- [ ] (사용자 확인 필요) OpenAI 키 / Vercel Blob 토큰 재발급 여부.
- [ ] 3단계(반 명단 문서 자동화 기능) 계획 문서 작성 — writing-plans 스킬로 진행.
- [x] 마지막 fix 커밋(51f13ca, a82ab3d) GitHub 푸시 완료, Vercel 배포 확인 완료 (dpl_JD5smPyr918rp2BsZgRMYDkpSX8Q, READY).

### 막힌 점 / 사용자 결정 필요

- 없음. 2단계는 완전히 완결된 상태.

### 참고 — Vercel 배포 주소 관련 사용자 혼동

- 사용자가 `toeic-summer-l7fkqoa8z-...vercel.app/grading` 같은 배포별 고유 주소로 들어가서 404를 봄 — 이건 예전 배포(그 시점 빌드)를 그대로 가리키는 고정 스냅샷 주소라서 생기는 정상 현상이지 버그 아님. Vercel 대시보드 Deployments 목록에서 옛날 항목을 클릭하면 이렇게 됨.
- 항상 최신 배포를 보려면 **https://toeic-summer.vercel.app** (Domains에 등록된 고정 도메인)만 쓰면 됨. 다음에 비슷한 "배포했는데 옛날 화면이 보인다"는 질문이 오면 이 설명을 먼저 확인.


## 2026-07-06

### 한 일

- 기존 설계 문서 2개(단어 시험 채점, 반 명단 문서 자동화) 재검토.
- 구현 계획을 3단계로 나누기로 결정: 1) 공통 기반(Next.js 프로젝트 + 공유 비밀번호 게이트) 2) 단어 시험 채점 기능 3) 반 명단 문서 자동화 기능.
- 1단계 계획 문서 작성 완료: `docs/superpowers/plans/2026-07-06-foundation-nextjs-password-gate.md`.

### 결정사항과 이유

- **3단계 분할 채택**: 기반부터 만들어서 실제로 배포/동작하는 걸 빨리 확인하고, 그 위에 기능을 하나씩 얹기로 함 (사용자 선택, "3단계로 분할 (추천)" 옵션).
- **인증 방식**: 세션/DB 없이 `SHARED_PASSWORD` 환경변수와 단순 비교 + httpOnly 쿠키(비밀번호의 SHA-256 해시)만 사용. 이 프로젝트는 개인정보를 지키는 인증이 아니라 익명 사용/비용 낭비 방지가 목적이라 이 정도 단순함이면 충분하다고 판단.
- **테스트 도구**: Vitest 선택. Next.js 미들웨어와 API 라우트 핸들러는 실제 Next 서버 없이도 `NextRequest`를 직접 생성해 함수 단위로 테스트 가능해서, 별도 e2e 도구 없이 이 방식으로 TDD 진행하기로 함.
- **새 패키지/환경변수는 실행 전 사용자 승인 필요** (CLAUDE.md 지침) — 계획 문서 안에 승인 필요 지점을 명시해둠 (`npm install`, `SHARED_PASSWORD` 값 설정, Vercel 환경변수 등록).

### 미완료 / 다음에 할 일 (최초 작성 시점 기준, 아래 이어서 갱신됨)

- [x] 공통 기반 계획(1단계) 실행 — subagent-driven 방식으로 진행 완료.
- [ ] 2단계(단어 시험 채점 기능) 계획 문서 작성 — 1단계 실행/배포 확인 후 진행.
- [ ] 3단계(반 명단 문서 자동화 기능) 계획 문서 작성.

---

## 2026-07-06 (이어서) — 1단계 구현 완료

### 한 일

- subagent-driven-development로 1단계 계획(Task 1~5) 전부 실행 및 태스크별 리뷰 통과.
  - Task 1: Next.js 스캐폴딩. Task 2: `lib/auth.ts` 비밀번호 검증(TDD). Task 3: 로그인 API+화면 (수정 2회: malformed JSON, null body 크래시). Task 4: 미들웨어 게이트(TDD).
- **수동 테스트로 실제 버그 발견**: `npm run dev`로 실서버를 띄워 확인하던 중 `GET /`가 500 에러. 원인은 Next.js 미들웨어가 Edge 런타임에서 도는데 `lib/auth.ts`가 Node 전용 `crypto.createHash`를 써서 발생. Vitest 단위 테스트는 Node 환경이라 이 문제를 못 잡았음 — **자동 테스트만 믿지 않고 실제로 서버를 띄워본 게 결정적이었음.**
  - 수정: `hashPassword`를 Web Crypto API(`crypto.subtle.digest`) 기반 비동기 함수로 전환, 호출부(`middleware.ts`, `app/api/login/route.ts`, 테스트 2개) 전부 `await` 추가. 커밋 `099808c`.
- 로컬에서 4가지 흐름(비로그인 리다이렉트/틀린 비밀번호/올바른 비밀번호/새로고침 유지) 전부 curl로 확인 완료. `npm run build` 성공.
- GitHub 저장소(`https://github.com/qkrtjdwo1231/toeic_summer`) 연결 후 첫 푸시, Vercel 배포까지 사용자가 직접 진행 완료. 배포된 사이트에서 비밀번호 게이트 정상 동작 확인.
  - 참고: 홈 화면의 "단어 시험 채점"/"반 명단 문서 자동화" 링크는 **아직 만들지 않은 2·3단계 기능**이라 지금 눌러도 404가 뜨는 게 정상. 버그 아님.
- 전체 브랜치 최종 리뷰(opus) 진행 → Important 2건(Edge 호환성 유지용 안내 주석 부재, `SHARED_PASSWORD` 설정 문서 부재) 발견 → 수정 서브에이전트로 `README.md`/`.env.example` 추가, `lib/auth.ts`에 안내 주석 추가, 계획 문서의 오래된 시그니처(`hashPassword`가 이제 비동기) 정정.
- `npm audit` 직접 확인: critical 취약점 1개 포함 총 7개 모두 devDependency(vite/vitest 체인)에서만 발생, 프로덕션 런타임(`next`/`react`, prod dependency 16개)에는 영향 없음. `vitest`를 4.x(semver major)로 올려야 해결되는데, 이건 새 버전 설치라 사용자 승인이 필요한 사안 — **지금은 손대지 않고 보류하기로 결정** (devDependency만 영향, 실사용에 위험 없음).

### 결정사항과 이유

- **Edge 런타임 호환성 문제는 계획 자체의 결함이었음**: 계획 문서 Task 2에 `Node 내장 crypto`라고 명시했던 게 원인. 앞으로 계획을 쓸 때 "이 코드가 미들웨어에서도 도는가?"를 미리 따져야 함 — 2·3단계 계획 작성 시 참고할 것.
- **npm audit 취약점은 지금 안 고침**: 전부 dev 전용(vitest/vite), 프로덕션 코드 경로에 없음. semver-major 업그레이드는 사용자 승인 필요 사안이라 임의로 진행하지 않음.

### 미완료 / 다음에 할 일

- [ ] 2단계(단어 시험 채점 기능) 계획 문서 작성 — writing-plans 스킬로 진행.
- [ ] 3단계(반 명단 문서 자동화 기능) 계획 문서 작성.
- [ ] (선택) `vitest` 메이저 업그레이드로 devDependency 취약점 해소 — 사용자 승인 필요, 급하지 않음.

### 막힌 점 / 사용자 결정 필요

- 없음. 1단계는 완결된 상태.
